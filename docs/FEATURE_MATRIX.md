# Feature matrix: claimed/expected vs implemented

The single source of truth for what libglot has, what it once claimed to
have, and what remains. Every DONE row is enforced by named tests; every
GAP row is either scheduled (issue/wave) or explicitly out of scope with
the rejection behavior documented. Update this file in the same PR as the
feature.

Legend: **DONE** (tested) · **GAP** (planned) · **OOS** (out of scope —
parser must fail cleanly, never silently mis-parse).

## SQL — statements and clauses

| Feature | Status | Evidence / plan |
|---|---|---|
| SELECT core (joins, subqueries, CTEs, set ops) | DONE | test_parser, test_cte_windows_subqueries, test_roundtrip_property |
| Window functions incl. real frames, GROUPS | DONE | test_unbounded_following |
| Named windows (`WINDOW w AS (...)`) | DONE | test_named_windows; `OVER w` references a `WINDOW` clause entry; `WINDOW` is a soft keyword (lexes as IDENTIFIER, no reserved-word cost) |
| GROUPING SETS / ROLLUP / CUBE (nested, empty set) | DONE | test_group_by_extensions |
| ORDER BY ... NULLS FIRST/LAST | DONE | test_order_by_nulls, test_roundtrip_property ("ORDER BY NULLS FIRST/LAST"); MySQL/MariaDB/SQLServer/AzureSynapse throw std::logic_error (no native syntax - chose "throw" over the ISNULL/CASE-prefix workaround) |
| DISTINCT ON (PostgreSQL) | DONE | test_distinct_on, test_roundtrip_property ("DISTINCT ON"); every other dialect throws std::logic_error |
| VALUES as table source (`FROM (VALUES ...) v(c1)`) | DONE | test_values_table_source, test_roundtrip_property corpus; reuses the previously-dormant `ValuesClause` node with an added alias + column list |
| USING / NATURAL joins | DONE | test_join_using_natural, test_roundtrip_property corpus |
| TABLESAMPLE | DONE | test_tablesample, test_roundtrip_property ("TABLESAMPLE"); fixed two bugs - the keyword check tested `TK::IDENTIFIER` but `TABLESAMPLE` lexes as its own reserved token (branch was dead code), and the `Tablesample` node had no field for the sampled table (silently discarded it). Added `REPEATABLE(seed)`. MySQL/MariaDB throw std::logic_error |
| QUALIFY | DONE | test_qualify, test_roundtrip_property ("QUALIFY"); fixed a silent-drop bug - `SelectStmt::qualify` parsed correctly but `visit_select_stmt` never read it back out, so QUALIFY vanished from generated SQL with no error. Now emitted for Snowflake/BigQuery/DuckDB; every other dialect throws std::logic_error |
| INTERVAL literals | DONE | test_interval_literals, test_roundtrip_property corpus; replaced a broken `FunctionCall("INTERVAL", ...)` encoding (regenerated as `INTERVAL(7, DAY)`, invalid SQL and not a fixed point) with a dedicated `IntervalLiteral` node covering both `INTERVAL '1 day'` and `INTERVAL '2' HOUR` / `INTERVAL 7 DAY` |
| INSERT ... ON CONFLICT (PG) / ON DUPLICATE KEY UPDATE (MySQL) | DONE | test_upsert, test_roundtrip_property ("upsert forms"); same-dialect fixpoint only - cross-dialect PG&lt;-&gt;MySQL transpile throws std::logic_error (conflict-target columns and EXCLUDED/VALUES() semantics don't map over cleanly) |
| MERGE (all WHEN arms) | DONE | test_bugfix_regressions |
| MERGE ... WHEN NOT MATCHED BY SOURCE (T-SQL) | DONE | test_merge_extended, test_roundtrip_property ("MERGE WHEN NOT MATCHED BY SOURCE"); `MergeStmt` reworked from single UPDATE/INSERT slots into an ordered `when_clauses` list (`MergeWhenClause`: match kind, optional `AND` condition, action) so WHEN MATCHED THEN DELETE and a WHEN MATCHED AND cond THEN ... condition are also modeled, not just the T-SQL-specific clause; NOT_MATCHED_BY_SOURCE throws std::logic_error outside SQL Server/Azure Synapse |
| OUTPUT / RETURNING (cross-dialect) | DONE | test_output_clause |
| CREATE TABLE full column/constraint schema | DONE | test_schema_type, test_fk_check_constraints |
| CREATE TABLE trailing table options (ENGINE=, DISTSTYLE, ...) | DONE | test_table_options, test_roundtrip_property ("CREATE TABLE trailing table options"); modeled as an ordered `(name, value, has_equals)` list on `CreateTableStmt`, regenerated verbatim - never dialect-gated (every dialect's own trailing syntax round-trips); name/value boundaries are a documented best-effort heuristic (a small whitelist of recognized option-start keywords, paren-depth aware) covering the forms in the spec, not a full per-dialect option grammar |
| CREATE/ALTER/DROP SEQUENCE, NEXTVAL/CURRVAL | DONE | test_sequences, test_roundtrip_property ("sequences"); NEXTVAL('seq')/CURRVAL('seq') function-style and Oracle's member-style `seq.NEXTVAL`/`seq.CURRVAL` both canonicalize to one `SequenceRefExpr` node, regenerated per dialect (Oracle member-style, function-style elsewhere); MySQL/SQLite throw std::logic_error (no sequence object) |
| Temporal tables (`FOR SYSTEM_TIME AS OF ...`) | DONE | test_temporal_tables, test_roundtrip_property ("temporal tables"); all four SQL:2011 forms (AS OF / FROM..TO / BETWEEN..AND / CONTAINED IN / ALL) parse onto `TableRef`; generates for SQL Server, Azure Synapse, MariaDB; every other dialect throws std::logic_error |
| CONNECT BY / START WITH (Oracle, Snowflake) | DONE | test_connect_by; non-native dialects throw |
| CONNECT BY → recursive CTE lowering | DONE (issue #2) | test_connect_by_lowering; `sql/include/libglot/sql/transforms.h::lower_connect_by` rewrites `START WITH s CONNECT BY [PRIOR] c` into `WITH RECURSIVE hierarchy AS (anchor WHERE s UNION ALL recursive JOIN hierarchy ON c')`, `PRIOR expr` in `c` qualified by the CTE (parent row), everything else by the source alias (child row); a `level` column is added only when `LEVEL` is referenced anywhere in the query; `WHERE` is re-applied on the outer SELECT (Oracle semantics: filtered after the hierarchy is built), never inside the anchor. Opt-in via `SQLGenerator(dialect, libglot::Arena* transform_arena)` - without an arena, non-Oracle/Snowflake dialects still throw std::logic_error as before (message now also points at the arena). No clean lowering exists for CONNECT BY NOCYCLE (needs a key column to build a cycle path, not derivable from syntax alone), ORDER SIBLINGS BY (depends on the traversal path), or a JOIN/multi-table FROM combined with CONNECT BY - all three throw std::logic_error with a specific message; CONNECT_BY_ROOT/SYS_CONNECT_BY_PATH are moot since the parser doesn't accept them in expression position at all |
| Procedural SQL (IF/WHILE/FOR, cursors, RAISE map) | DONE | test_procedure_dialects, test_for_keyword |
| FOR record IN SELECT loops, REVERSE | DONE | test_for_keyword, test_roundtrip_property ("FOR record/REVERSE loop forms"); `ForLoop` extended with `reverse` and a `query` slot (mutually exclusive with the range form); record iteration generates PostgreSQL's bare `FOR rec IN SELECT ...` and Oracle's parenthesized `FOR rec IN (SELECT ...)`, and throws std::logic_error for the T-SQL lowering (no direct equivalent); REVERSE lowers to a descending WHILE for T-SQL |
| GRANT/REVOKE, transactions, utility stmts | DONE | test_grant_revoke, test_utility_statements |
| XML functions (SQL:2003) | OOS | clean ParseError; revisit on demand |
| Polymorphic table functions (SQL:2016) | OOS | clean ParseError |
| Oracle PL/SQL packages | OOS | clean ParseError |

## SQL — dialect-specific constructs

| Feature | Status | Evidence / plan |
|---|---|---|
| Dialect-aware lexing (TokenizerConfig) | DONE | test_tokenizer |
| LIMIT / TOP / OFFSET-FETCH / FIRST-SKIP mapping | DONE | test_bugfix_regressions, test_dialect_feature_combinations |
| Boolean spelling, quoting styles, ILIKE polyfill | DONE | test_dialect_feature_combinations |
| MySQL fulltext `MATCH ... AGAINST` | DONE | test_fulltext_match, test_roundtrip_property ("MySQL fulltext"); dedicated `MatchAgainst` node covers all four AGAINST modifiers (bare/NATURAL LANGUAGE MODE/+WITH QUERY EXPANSION/BOOLEAN MODE/WITH QUERY EXPANSION alone); MySQL/MariaDB only, everything else throws std::logic_error. Parsing the search argument required suppressing the generic `expr IN (...)` postfix (a scoped `no_in_postfix_` flag) so `AGAINST('x' IN NATURAL LANGUAGE MODE)` doesn't misparse "IN" as the value-list operator |
| BigQuery STRUCT literal / ARRAY subscript edge cases | DONE | test_struct_array_subscript, test_roundtrip_property ("BigQuery STRUCT ... array subscript"); `STRUCT(...)` (already parsed generically as a FunctionCall) now throws std::logic_error for every dialect but BigQuery at generation time; `ArrayIndex` gained a `subscript` field (NONE/OFFSET/ORDINAL/SAFE_OFFSET) so `arr[OFFSET(0)]`/`arr[ORDINAL(1)]`/`arr[SAFE_OFFSET(0)]` generate only for BigQuery while plain `arr[index]` is untouched everywhere. Required adding a BigQuery `TokenizerConfig` (bracket_identifiers=false) - BigQuery previously inherited the ANSI default bracket-quoted-identifier lexing, which made `identifier[...]` unparseable as a subscript at all; PostgreSQL/MySQL/ANSI still can't lex bare `ident[...]` subscripting (pre-existing, asserted in test_tokenizer.cpp) and are out of scope here |
| Snowflake `FLATTEN` table function | DONE | test_flatten, test_roundtrip_property ("Snowflake LATERAL FLATTEN"); `LATERAL FLATTEN(INPUT => expr [, PATH => '...'] [, OUTER => bool])` parses onto a dedicated `FlattenClause` wrapped in the existing `LateralJoin` node; required a new `=>` token (FAT_ARROW) in the tokenizer. Snowflake only; every other dialect throws std::logic_error |
| PG `?` key-exists fixpoint (lexes as operator) | DONE (documented exclusion) | test_roundtrip_property header |
| First-class set (22): ANSI, PG, MySQL, SQLite, MSSQL, Snowflake, Oracle, DB2, BigQuery, DuckDB, Redshift, Greenplum, TimescaleDB, CockroachDB, YugabyteDB, Citus, RisingWave, Materialize, MariaDB, TiDB, SingleStore, AzureSynapse | DONE | test_dialect_feature_combinations, test_dialect_{oracle,db2,bigquery,duckdb,pg_family,mysql_family,tsql_family}, test_roundtrip_property. The remaining ~23 dialects in the enum (Informix, Firebird, SAP HANA, Dremio, MonetDB, Drill, Spanner, QuestDB, H2, HSQLDB, Derby, ClickHouse, Teradata, Vertica, Netezza, Exasol, Presto, Trino, Athena, Hive, Impala, SparkSQL, Databricks) are honestly labelled quoting/traits-only in dialect_traits.h and are not claimed as first-class - see docs/ROADMAP.md stage 2 |
| Promote Oracle, DB2, BigQuery, DuckDB | DONE (issue #3) | one conformance suite per dialect with exact-string roundtrips and fixpoints: test_dialect_oracle, test_dialect_db2, test_dialect_bigquery, test_dialect_duckdb |
| Promote the 12 family-member dialects (issue #3 follow-on, stage 2) | DONE | test_dialect_pg_family (Redshift, Greenplum, TimescaleDB, CockroachDB, YugabyteDB, Citus, RisingWave, Materialize), test_dialect_mysql_family (MariaDB, TiDB, SingleStore), test_dialect_tsql_family (AzureSynapse), test_roundtrip_property. Real per-dialect deltas encoded (each with a negative/throws test proving the restriction and a fixpoint proving the positive form): Redshift/Greenplum DISTSTYLE/DISTKEY/SORTKEY/DISTRIBUTED BY table options and the SUPER column type (already generic, just needed tests); CockroachDB `AS OF SYSTEM TIME <expr>` (new `TableRef::as_of_system_time`) and `UPSERT INTO ...` (new `InsertStmt::is_upsert` - previously silently downgraded to a plain INSERT on regeneration, not even a fixed point; fixed here); RisingWave `SELECT ... EMIT CHANGES` (new `SelectStmt::emit_changes`) and `CREATE MATERIALIZED VIEW` (new `CreateViewStmt`/`DropViewStmt::materialized`, gated to the whole PostgreSQL family); Materialize `TAIL`/`SUBSCRIBE` (new `ShowStmt::is_tail`/`is_subscribe` - fixes a pre-existing bug where `TAIL` had no dedicated generator branch and silently regenerated as `SHOW <name>`) and `CREATE MATERIALIZED VIEW`; MariaDB `CREATE/DROP/ALTER SEQUENCE` + `NEXTVAL`/`LASTVAL` with a bare identifier argument (not PostgreSQL's quoted-string `nextval('seq')` convention) and `RETURNING` on INSERT/DELETE only (never UPDATE) - encoding this honestly required first fixing a pre-existing gap where the generic RETURNING path had no dialect gate at all, so MySQL itself would have silently emitted invalid `RETURNING` SQL; TiDB/SingleStore's previous "TRUE"/"FALSE" boolean-literal override (issue #5's illustrative delta example) was found unconfirmable while promoting them and reverted to inherit MySQL's 1/0 per the rules of honesty. Azure Synapse: kept deliberately identical to SQL Server for MERGE and OUTPUT (reported version/SKU-dependent gaps in real Synapse were not confirmed with enough certainty to encode as a restriction - guessing wrong would be worse than staying inherited). Deliberately NOT extended (documented uncertainty, not guessed at): Redshift's JSON operator support (`->`/`->>`/etc. stay unrestricted - whether Redshift's operator set exactly matches PostgreSQL's was not verified); YugabyteDB's own point-in-time read story (not confirmed to share CockroachDB's exact `AS OF SYSTEM TIME` syntax); TiDB/SingleStore sequence and RETURNING support (left on the generic inherited default, neither asserted supported nor asserted absent); DISTINCT ON (stays gated to literally `SQLDialect::PostgreSQL`, not widened to the family, since only Redshift's non-support was confirmed) |
| Dialect family inheritance (base profile + delta) | DONE (issue #5) | test_dialect_families; `dialect_traits.h` adds a `SQLDialectFamily` enum (Standard, PostgreSQL, MySQL, TSQL, Oracle, DB2, BigQuery, DuckDB, Snowflake, SQLite - only families the code actually distinguishes; Hive/Presto were considered and dropped, nothing branches on them today), a `family` field on `SQLFeatures`, one `constexpr <family>_base()` per family (the flagship dialect's exact feature vector), and a `with(base, {delta})` combinator so each of the 45 `kFeatures` rows states only what differs from its family - all still a `constexpr` compile-time table, zero runtime cost. `SQLDialectTraits::family(id)` / `is_family(id, fam)` are the query surface; `generator.h` now expresses genuinely family-wide branches (e.g. T-SQL's OUTPUT clause, MERGE/DECLARE/WHILE/FOR/RAISE T-SQL forms) as `is_family(d, TSQL)` instead of repeating `d == SQLServer \|\| d == AzureSynapse` at each call site. Branches that are *not* actually family-wide (a single member borrowing another family's syntax, e.g. MariaDB's T-SQL-style temporal tables; or MySQL-family checks that would newly rope in TiDB/SingleStore) were deliberately left as explicit dialect lists, each with a comment explaining why converting would be a silent behavior change. Confirmed behavior-preserving by dumping every dialect's full `SQLFeatures` before and after the rewrite and diffing byte-for-byte (empty diff). PostgreSQL family's 8 real forks (Redshift, Greenplum, CockroachDB, YugabyteDB, Citus, TimescaleDB, RisingWave, Materialize), MySQL family's MariaDB/TiDB/SingleStore, and T-SQL family's AzureSynapse are now all first-class - see the stage-2 promotion row above |

## SQL — optimizer

| Feature | Status | Evidence / plan |
|---|---|---|
| Constant folding (int, string ||) with guards | DONE | test_optimizer |
| Boolean simplification | DONE | test_optimizer |
| WHERE TRUE pruning | DONE | test_optimizer |
| Predicate/projection pushdown, join reordering | OOS | old claims; needs schema/cardinality model to be real |

## MIME

| Feature | Status | Evidence / plan |
|---|---|---|
| Single pipeline entry (`parse_message`) w/ policies | DONE | test_pipeline, test_mime_anomalies |
| CRLF/LF, folded headers, comments, address groups | DONE | test_mime_parser, test_header_comments, test_address_groups |
| RFC 5322 §4 obsolete header grammar (WSP before/around `:`, blank lines mid-fold) tolerance | DONE | test_rfc_conformance_mime4j (obsolete.msg and the malformedHeader*/malformedHeaderStartsBody*.msg fixtures); `parse_header_with_parameters` (parser_extended.h) recovers from a header line that isn't well-formed field-name+`:`+value (skips to the next NEWLINE, records `AnomalyKind::ObsoleteHeaderSyntax`) instead of aborting the whole message - found while importing the mime4j suite that the obs-* grammar itself already tokenized correctly (unfolding and WSP-around-`:` both already worked); the actual blocker was a single malformed line (e.g. a non-ASCII field name) taking down the entire parse. Measured real corpus impact: raw SpamAssassin parse rate 98.61% -> 99.58%, full Enron 99.99% -> 100.00%. See docs/ROADMAP.md |
| RFC 2046 multipart (anchored boundaries, limits) | DONE | test_boundary_recovery, test_mime_multipart |
| RFC 2046 §5.1.1 preamble/epilogue | DONE | test_rfc_conformance_mime4j; `Message::preamble`/`epilogue` (ast_nodes.h), populated by `parse_multipart_body` (parser_extended.h) regardless of whether the first recognized delimiter opens a part or is itself an immediate close (0 body-parts, which RFC 2046 permits); found missing while importing the mime4j conformance suite (docs/ROADMAP.md) - a multipart with only a close delimiter and no preceding open previously looked like "undivided content" only because this was never surfaced, not because the 0-parts result was wrong (mime4j reports 0 parts for the same fixtures). Exposed in tools/mime_dump.cpp's JSON schema |
| RFC 2231 continuations (decode) | DONE | test_rfc2231_continuations |
| base64 / quoted-printable **decode** (strict) | DONE | test_mime_encoding |
| base64 / quoted-printable / RFC 2047 **encode** | DONE | test_mime_encoding; `TransferEncoding::encode_base64` (RFC 2045, 76-char CRLF-wrapped, exact-string + binary-data round-trip + 75/76/77-char wrap-boundary cases) and `encode_base64_raw` (unwrapped, used standalone and by encoded-words); `TransferEncoding::encode_quoted_printable` (non-printables and `=` escaped, trailing space/tab escaped, existing CR/LF passed through untouched as hard breaks, soft `=\r\n` breaks so no line exceeds 76 cols, 75/76/77-char boundary cases); `EncodedWordDecoder::encode_word` (RFC 2047 `=?UTF-8?B?...?=` / `?Q?`, splits into multiple encoded-words on the 75-char limit at UTF-8 codepoint boundaries, non-ASCII-subject and emoji round-trip tests) |
| Charsets: ISO-8859-1, ISO-8859-15 (Latin-9), ISO-8859-9 (Latin-5, Turkish), ISO-8859-2 (Latin-2, Central European), KOI8-R (Cyrillic), Windows-1252 → UTF-8 | DONE | test_mime_encoding, test_charset_latin9, test_charset_regional; ISO-8859-9/-2 and KOI8-R (roadmap stage 5 differential follow-up) found missing by running the raw SpamAssassin differential residual field-by-field: real messages in the 500-sample used these charsets and libglot reported them undecodable while Python decoded them fine. ISO-8859-9 is a Latin-1 delta (6 Turkish substitutions, like Latin-9's 8); ISO-8859-2 and KOI8-R are not Latin-1 deltas and get their own 128-entry tables, generated from Python's own codecs and cross-checked byte-for-byte. Wired into all three charset dispatch points that previously had to be kept in sync by hand (`decoded_body_utf8`, `EncodedWordDecoder::decode`'s RFC 2047 path, and `CharsetConverter::to_utf8`) - missing the `EncodedWordDecoder` one was caught by a dedicated encoded-word test per charset, not by inspection. Raw SpamAssassin corpus (3,303 messages, mbox-split): text-decode rate 98.95% -> 99.25% |
| UTF-16 (BE/LE, BOM) → UTF-8 | DONE | test_charset_utf16; `CharsetConverter::utf16_to_utf8` (RFC 2781) - FEFF/FFFE BOM detection (consumed, overrides the passed-in default), big-endian default per RFC 2781 when no BOM, surrogate-pair combination (emoji), unpaired high/low surrogates and a truncated trailing byte replaced with U+FFFD (never throws, output re-validated with `is_valid_utf8`); wired into `Charset::UTF16`/`UTF16BE`/`UTF16LE` (`to_utf8`) and `decoded_body_utf8()` so `charset=UTF-16`/`UTF-16BE`/`UTF-16LE` parts decode through the normal pipeline |
| Asian charsets (Shift-JIS, EUC-KR, GB2312) | OOS | reported as unknown-charset, never mislabeled |
| message/partial detection | DONE | test_message_partial; `Content-Type: message/partial` detected in `finish_message` (parser_extended.h), `id`/`number`/`total` parsed onto a new `MessagePartialRef` (complete_features.h, `Message::message_partial`) with `std::from_chars`-based defensive numeric parsing (malformed/negative/overflowing values default to 0, never throws); records the new `AnomalyKind::MessagePartialDetected` (Structural severity) so callers know reassembly with sibling fragments is required; absent for normal messages and for `message/external-body`; reassembly itself is out of scope |
| `message/rfc822` nesting (RFC 2046 §5.2.1) | DONE (issue #6) | test_message_rfc822, test_rfc_conformance_mime4j; `finish_message` (parser_extended.h) recurses into the body of a `message/rfc822` part via `parse_encapsulated_message`, reusing the same header+body pipeline as multipart parts (`parse_part`) and attaching the result to `Message::encapsulated` (ast_nodes.h). Enforces the identical `max_nesting_depth`/`max_total_parts` DoS limits and tracker as multipart, recording `AnomalyKind::ExcessiveNestingDepth`/`ExcessivePartCount` (Reject/DoS under the standard config) so a chain of nested `message/rfc822` cannot recurse unbounded. Covers: forwarded mail with headers+body, 2-deep nesting, a `message/rfc822` part inside `multipart/mixed`, and depth-limit enforcement. `transfer_decoded_rfc822_body` (found via the mime4j conformance suite, docs/ROADMAP.md) transfer-decodes the part's own base64/quoted-printable Content-Transfer-Encoding before recursing -- RFC 2046 §5.2.1 permits only 7bit/8bit/binary there, but real senders base64-encode message/rfc822 bodies anyway, and parsing the still-encoded bytes directly as headers+body silently produced an empty nested message rather than the real, recoverable content |
| `Date:` parsing (RFC 5322 §3.3 date-time) | DONE (issue #6) | test_date_parsing; `DateTimeParser` (complete_features.h) hand-parses the full date-time grammar (optional day-of-week, day/month/year, time-of-day, zone) into `ParsedDateTime` (year/month/day/hour/min/sec/tz_offset_minutes + the original `raw` string_view), attached to `Message::date` by `parse_date_header` (parser_extended.h) for every message/part regardless of Content-Type. Handles RFC 5322 §4.3 obsolete forms: 2-digit years (00-49→20xx, 50-99→19xx) and 3-digit years (→19xx); named zones UT/GMT (+0000, known) and EST/EDT/CST/CDT/MST/MDT/PST/PDT (fixed offsets); the single-letter military zones and any other obs-zone letters, all folded to `tz_unknown=true`/offset 0 per §4.3's "SHOULD all be considered equivalent to '-0000'"; `-0000` itself is parsed as offset 0 with `tz_unknown=true` (§3.3, vs. `+0000` which is known-UTC); leap seconds (`sec=60`) accepted; folded values already unfolded and comments already stripped upstream (Date is in `is_structured_field`). Invalid dates (bad month name, day 32, Feb 30/29-on-non-leap-year, hour>23, missing zone, zone minutes>59, ...) never throw -- `valid=false` and `AnomalyKind::InvalidDateFormat` (pre-existing kind, previously unwired) is recorded, `Message::date` stays nullptr |
| `multipart/report` (RFC 6522) / `message/delivery-status` (RFC 3464) | DONE (issue #6) | test_multipart_report; `multipart/report` is detected in `finish_message` (it already splits generically as `multipart/*`), and the required `report-type` parameter (RFC 6522 §4) is validated, recording the new `AnomalyKind::MissingReportTypeParameter` (Structural) when absent. `message/delivery-status` parts are recognized and their body parsed by `DeliveryStatusParser` (complete_features.h) into `Message::delivery_status`: RFC 3464 §2.1's per-message field group (first blank-line-delimited block) followed by one per-recipient field group per recipient, with folded continuation lines joined. Tested end-to-end over a realistic bounce (human-readable part + `message/delivery-status` + original `message/rfc822`) |
| Message-ID / In-Reply-To / References (RFC 5322 §3.6.4 msg-id) | DONE (issue #6) | test_threading_headers; `MessageIdParser` (complete_features.h) parses `<local@domain>` msg-id tokens into `MessageId {value, valid}`, wired by `parse_threading_headers` (parser_extended.h) onto `Message::message_id` (single), `in_reply_to`/`references` (lists, References/In-Reply-To allow `1*msg-id`). Folding is already undone and comments already stripped upstream (these three fields are in `is_structured_field`). Malformed candidates (missing angle brackets entirely, unterminated `<foo@bar`, missing `@`, empty local-part/domain, nested `<<...>>`, disallowed characters) never throw: each is recorded as `AnomalyKind::InvalidMessageIdSyntax` (new kind) and surfaced with `valid=false` rather than silently dropped |
| `multipart/related` `start` (RFC 2387 §3.4) + Content-ID/Location/Description/Language | DONE (issue #6) | test_related_content_headers; `resolve_related_start` (parser_extended.h) resolves the optional `start` parameter against each part's Content-ID header (both normalized via angle-bracket/quote stripping) and attaches the match as `Message::related_root`; absent or unresolved `start` falls back to the first part (RFC 2387 §3.4), the unresolved case additionally recording the new `AnomalyKind::InvalidRelatedStart`. Content-ID/Content-Location/Content-Description/Content-Language added to `is_structured_field` so RFC 5322 comments are now stripped for them too (previously only Content-Disposition was handled) |
| RFC 6532 internationalized (raw UTF-8) headers | DONE (issue #6) | test_utf8_headers; every header value is checked in `enhance_header` (parser_extended.h): bytes ≥ 0x80 are legal per RFC 6532 and are never modified (header values are always plain arena-owned slices, so they survive intact regardless); only genuinely ill-formed UTF-8 (verified with the existing `CharsetConverter::is_valid_utf8`) is flagged, via the new `AnomalyKind::InvalidUtf8Header` (Security severity - parser-confusion potential, consistent with `CharsetMismatch`/`NullByteInHeader`). Covers valid raw-UTF-8 Subject/From display names, invalid continuation bytes, and a stray continuation byte |
| `multipart/signed` / `multipart/encrypted` (RFC 1847) | DONE (issue #6), signature verification OOS | test_multipart_signed; the required `protocol` parameter (RFC 1847 §2) is validated for both types in `finish_message`, recording the new `AnomalyKind::MissingProtocolParameter` when absent. Byte-exact preservation: `Message::raw_source` (ast_nodes.h) is populated in `parse_part` (parser_extended.h) with the exact transmitted bytes of a part's headers+body, sliced directly from the arena-owned source *before* header unfolding -- proven by a test where the signed part's Content-Type is folded across two lines and has trailing whitespace before the boundary: the parsed `Header::value` is unfolded/normalized as usual, but `raw_source` retains the literal fold and trailing whitespace untouched. **Signature verification itself is out of scope** (no crypto dependency): libglot only guarantees this span is never normalized/re-encoded, so a caller with a crypto library can verify against it |
| Corpus benchmark (SpamAssassin) | DONE (issue #4) | tools/mime_corpus runs any message directory through the pipeline and reports parse success, policy rejections, text-decode rate and an anomaly histogram; exits non-zero below --min-success. CI: committed corpus (tests/corpus/mime) gated at 100%, SpamAssassin public corpus run best-effort and reported |

## Engineering standards

| Item | Status | Plan |
|---|---|---|
| Differential testing vs Python `email` | DONE (issue #7) | tools/mime_dump emits a canonical JSON structure; scripts/mime_diff.py builds the same structure from Python's stdlib email (BytesParser, policy.default) and diffs field by field. CI job `mime-differential` gates the committed corpus at 100%. Measured on 500 real SpamAssassin messages: **92.2% agreement**. Residual classified (see docs/ROADMAP.md stage 4): ISO-8859-15 bodies libglot does not decode (charset scope, issue #8); us-ascii-declared bodies containing 8-bit bytes, where Python's strict decode fails and libglot passes the bytes through; a malformed date zone (`01800`) Python resolves to +18:00 and libglot leaves unparsed; address/subject formatting conventions. No libglot correctness bug found by the oracle at this point in the investigation - the field-by-field classification of a later, raw-corpus sample did find and fix two (a header rejection wrongly suppressing Date/Message-ID parsing, and a date-zone parser accepting trailing garbage), see docs/ROADMAP.md stage 5's "Differential residual, classified field-by-field" |
| CI: GCC+Clang, ASan/UBSan, Werror, install test | DONE | .github/workflows/ci.yml |
| Fuzzers (parser, roundtrip contract, MIME) | DONE | fuzz/ |
| Security / adversarial-input corpus (MIME) | DONE | test_security_corpus.cpp; hand-crafted attack shapes (null-byte smuggling, filename path traversal, RFC 2047-encoding evasion of the filename check, boundary confusion) run deterministically in CI, distinct from fuzz_mime_parser's randomized mutation fuzzing. Found and fixed five genuinely dead anomaly detectors (`NullByteInHeader`, `NullInBase64`, `InvalidFilenameChars`, `DuplicateFilenameParameter`, `ExcessiveFilenameLength` - all defined in anomalies.h with severity classification but never actually raised anywhere; `ParserLimits::max_filename_length` was likewise defined but unchecked, now backing `ExcessiveFilenameLength`); verified zero false positives over the full Enron + raw SpamAssassin corpora, with `InvalidFilenameChars` catching a real MHT-style path-embedded attachment name in the latter. See docs/ROADMAP.md |
| Coverage report in CI | DONE | ci.yml coverage job |
| Benchmarks re-run with current code, numbers recorded | DONE | bench/RESULTS_2026-07.md |
| Repo-wide clang-format + .git-blame-ignore-revs | DONE | style commit listed in .git-blame-ignore-revs; `git config blame.ignoreRevsFile .git-blame-ignore-revs` |
| clang-tidy | DONE | blocking CI job (clang-tidy-18, warnings-as-errors) over every first-party TU, which transitively covers the whole public header surface. Correctness findings fixed: int-widening in mime/limits.h and core/util/arena.h size constants, char-narrowing in mime/encoding.h and sql/lex/keywords.h, vestigial cross-namespace forward declarations in sql/lex/fwd.h, exception escaping from an example's main. Style-tier checks that conflict with the project's deliberate idiom (constexpr C-arrays, constructor init lists, single-statement ifs) are disabled in .clang-tidy with rationale; fuzz/.clang-tidy scopes off bugprone-empty-catch, since swallowing expected parse errors is the point of a fuzz target |
| SECURITY.md (reporting, threat model) | DONE | SECURITY.md |
| Doxygen config for public headers | DONE | Doxyfile (output docs/api/) |
